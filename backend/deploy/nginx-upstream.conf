# Managed by /usr/local/sbin/games-arena-switch-upstream.
# Port 3000 is the legacy slot used only for the first blue/green migration.
upstream games_arena_backend {
    server 127.0.0.1:3000 max_fails=3 fail_timeout=5s;
    keepalive 32;
}
